# [功能/模块名称] 规格说明书 (AI-Native Spec)

> **意图账本双向锚定 (Bidirectional Traceability)**
> 📌 **原始意图溯源**：[必须指向 `INTENTS.md` 中的具体日期或意图编号，例如：`2026-04-16 意图 3`]
> 🔄 **修订历史 (Changelog)**：
> - v1 (YYYY-MM-DD): 初始创建，承接 [意图X]，废弃了旧版逻辑。

---

## 1. 核心意图 (Core Intent)
> _一句话说明本模块/功能的存在意义。它在系统链路中扮演什么角色？解决了用户的什么原始痛点？_
- **目标**：...
- **场景**：...

## 2. 边界约束 (Constraints)
> _为后续接手该模块的 Agent 划定的绝对红线：明确“什么绝对不能做”。_
- 🚫 **禁区 1**：绝对不能跨越 `xxx` 边界去修改 `yyy` 文件。
- 🚫 **禁区 2**：...

## 3. 状态契约 (State Contract)
> _输入输出定义与副作用声明。基于意图导向，系统变更前后的预期状态是什么？_
- **前置依赖 (Inputs/Deps)**：...
- **预期副作用 (Side Effects)**：...

## 4. 验收标准 (Acceptance Criteria)
> _Agent 如何通过物理测试（如代码命令、自动化脚本）向人类自我证明已 100% 实现了该 Spec？_
- [ ] **标准 1**：运行 `claw test xxx` 命令，终端必须输出 `Success`。
- [ ] **标准 2**：...
